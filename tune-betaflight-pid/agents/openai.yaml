interface:
  display_name: "Betaflight PID 调试"
  short_description: "从黑盒 BBL 诊断共振并生成安全的 PID 调参 CLI"
  default_prompt: "Use $tune-betaflight-pid to analyze these Betaflight .bbl logs and produce a staged, evidence-based CLI tune."
